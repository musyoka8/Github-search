export class User {
    constructor(public name: string, public username:string, public followers:number, public following: number, public repositories:number, public user, public profile:string, public date: Date){

    }
}
